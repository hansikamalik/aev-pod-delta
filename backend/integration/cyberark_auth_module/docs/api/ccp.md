---

## `docs/api/ccp.md`
```markdown
# API Reference: CCPClient

Direct interface for CyberArk Central Credential Provider (CCP) AIM Web Service.

## Constructor

```python
CCPClient(ccp_url: str, app_id: str, verify_ssl: bool = True)
