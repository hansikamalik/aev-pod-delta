[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cyberark-discovery-integration"
version = "0.1.0"
dependencies = [
    "requests-mock"
]

[tool.setuptools.packages.find]
where = ["."]
